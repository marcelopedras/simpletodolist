<%=form_tag('/find_favorite', :remote => true, :id=>'search_list_form') do %>
    <%=search_field_tag('search', value = nil, options = {})%>
    <%= submit_tag 'Search in Public Lists', :id=>'submit_search_button' %>
<%end%>